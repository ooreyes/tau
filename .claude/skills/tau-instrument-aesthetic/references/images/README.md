# Reference images

Drop the source plates here with these exact filenames — `imagery.md` refers to
them by name:

- `01-complications-grid.png`
- `02-complications-dense.png`
- `03-radial-set.png`
- `04-instrument-cluster.png`
- `05-complications-large.png`

They arrived as chat attachments rather than files, so they are not committed
yet. `references/imagery.md` describes each plate in enough detail to design
from without them, but the written description is a summary — look at the
actual images when they are here.

Keep them lossless (PNG) and full resolution. Plate 5 in particular is used for
reading exact optical sizes and label spacing, which JPEG artefacts destroy.
