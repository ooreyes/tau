{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the standalone Settings window. Deliberately narrower than the main window's: Settings reads and writes preferences and keychain entries through Tau's own commands, which need no plugin permission, so the only core surface it needs is the window's own lifecycle. It gets no filesystem access - it never opens a schematic.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging"
  ]
}
